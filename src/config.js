/**
 * App configuration
 * Toggle features here without changing code paths elsewhere.
 */

export const config = {
  /**
   * When true, Barba.js is used for SPA-like page transitions (no full reload).
   * Requires Barba wrapper/container markup in Webflow (see README).
   * When false, the app runs in static mode (current behavior).
   */
  enableBarba: true,
};
